const Factory = artifacts.require('Factory')
const RouterV2 = artifacts.require('RouterV2')
const WETH = artifacts.require('WETH')
const Storage = artifacts.require('Storage')
const Registry = artifacts.require('Registry')
const Multicall2 = artifacts.require('Multicall2')

module.exports = async (deployer, network, accounts) => {
  // await deployer.deploy(WETH)
  // await deployer.deploy(Multicall2)
  // await deployer.deploy(Registry)
  // await deployer.deploy(Storage, accounts[0])
  // await deployer.deploy(WETH, accounts[0])
  // const weth = await WETH.deployed()
  // await deployer.deploy(Factory, accounts[0])
  // const factory = await Factory.deployed()
  // await deployer.deploy(RouterV2, factory.address, weth.address)
  // await RouterV2.deployed()
}
